import { combineReducers } from 'redux';
import postCustomPricingReducer from './postCustomPricingReducer';

const appReducer = combineReducers({
  postCustomPricingReducer
})

export default appReducer;
